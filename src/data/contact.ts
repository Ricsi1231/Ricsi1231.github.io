/**
 * The one address the site publishes. Referenced by the footer on every page and
 * by the courses page, so it lives here rather than being repeated in each.
 */
export const CONTACT_EMAIL = 'richardnagy551@gmail.com';
